# Security Policy

请不要在公开 Issue 中上传个人声音模型、未发布歌曲、访问令牌、私人路径日志或其他敏感文件。

本项目只在 `127.0.0.1` 启动本地服务。不要把端口映射到公网，也不要使用 Gradio `share=True`，除非你理解身份验证和文件泄露风险。

如果发现代码执行、任意文件访问或依赖下载安全问题，请通过 GitHub Security Advisory 私下报告。
